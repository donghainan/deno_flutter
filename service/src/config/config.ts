const config = {
  hostname: "localhost",
  username: "root",
  password: "root",
  db: "deno_flutter",
  poolSize: 3,
};

const HOST = '127.0.0.1'
const PORT = 3000;
const BASE_URL = "";
export { HOST, PORT, BASE_URL, config };
